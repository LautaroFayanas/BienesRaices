extends ../layout/index

block contenido 
    div.py-1
        h1.text-4xl.my-10.font-extrabold.text-center Bienes
            span.font-normal Raices
        h2.text-center.text-2xl.font-extrabold= pagina
        

    div(class='shadow py-8 px-4 rounded-lg max-w-md mx-auto')    
        p(class= error ? 'errorMessage' : 'successMessage').py-2.px-5.rounded-lg.w-full.font-bold.text-center.uppercase.text-white= mensaje

        if !error
            a(href='/auth/login' class='text-center font-bold text-sm text-slate-600 uppercase mt-10 block') Ya puedes iniciar sesion =)
        