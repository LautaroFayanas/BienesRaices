extends ./layout/app

block contenido        

        section(class='py-5 mx-auto w-4/5')
            h2(class='text-center text-4xl font-bold py-5')= pagina

            if propiedades.length
                div(class='grid grid-cols-1 lg:grid-cols-3 gap-5 py-10 mt-10')
                    each propiedad in propiedades
                        include ./templates/card
            
            else
                p(class='text-center text-gray-800 text-sm py-10') No hay resultados.
                    